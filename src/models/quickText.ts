class QuickText {
  text: string;
  sort: number;
  id: number;

  constructor(text: string, sort: number, id: number) {
    this.text = text;
    this.sort = sort;
    this.id = id;
  }
}
export default QuickText;
